# dependencies
/node_modules
/.pnp
.pnp.js

# next
/.next/
/out/
/build
next-env.d.ts

# production
/dist

# env — NEVER commit. See .env.example
.env
.env.local
.env.*.local
.env.development.local
.env.production.local

# Gemini working files — briefs stay in git, raw generations do not.
/assets/generated/
# NOTE: public/img/photos/ AND data/image-manifest.json ARE committed (derived but
# stable) so `next build` works on a fresh checkout / on Vercel without running the
# sharp+python image pipeline. Re-run `npm run images:process` when source photos change.

# misc
.DS_Store
*.pem
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.vercel
*.tsbuildinfo

# local audit / report output
/reports/
/data/baseline/

# Client source material, kept locally, not committed (too large / not site assets).
# The WordPress export is 38MB; the uploads folder is the old wp-content dump.
# Useful photos have been pulled into assets/source-photos/ and processed.
/allsafeelectric.WordPress.*.xml
/uploads/
# The high-res logo lives at public/img/brand/allsafe-electric-logo.webp (committed);
# the loose root copy is not needed in git.
/allsafe-electrician-LOGO.webp
