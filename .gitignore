# dependencies
/node_modules
/.pnp
.pnp.js

# next
/.next/
/out/
/build
next-env.d.ts

# production
/dist

# env — NEVER commit. See .env.example
.env
.env.local
.env.*.local
.env.development.local
.env.production.local

# Gemini working files — briefs stay in git, raw generations do not.
/assets/generated/
# NOTE: public/img/photos/ AND data/image-manifest.json ARE committed (derived but
# stable) so `next build` works on a fresh checkout / on Vercel without running the
# sharp+python image pipeline. Re-run `npm run images:process` when source photos change.

# misc
.DS_Store
*.pem
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.vercel
*.tsbuildinfo

# local audit / report output
/reports/
/data/baseline/
